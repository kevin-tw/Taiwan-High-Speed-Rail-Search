import React from 'react'
import { Layout } from 'antd'

const { Header } = Layout

const AppHeader = () => (
  <Header className="header">
    <div className="logo" />
  </Header>
)

export default AppHeader